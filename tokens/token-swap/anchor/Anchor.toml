[toolchain]

[features]
resolution = true
skip-lint = false

[programs.devnet]
swap_example = "67TZtJh41ogmUTij3zPxH1buWhf9q7TsrDkNUpD9c86k"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "Localnet"
# cluster = "devnet"
wallet = "~/.config/solana/id.json"

[scripts]
# test = "pnpm ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
# test = "pnpm ts-mocha -p ./tsconfig.json -t 1000000 tests/create-amm.ts"
test = "pnpm ts-mocha -p ./tsconfig.json -t 1000000 tests/swap.ts"

[test]
startup_wait = 5000
shutdown_wait = 2000
upgradeable = false
